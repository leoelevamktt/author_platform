name: Enable advanced admin

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  enable:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Attach advanced admin module
        run: >-
          python -c "from pathlib import Path; p=Path('index.html'); s=p.read_text(encoding='utf-8'); tag='<script src=\"/admin-enhancements.js?v=12\"></script>'; s=s if tag in s else s.replace('</body>',tag+'\\n</body>'); p.write_text(s,encoding='utf-8')"
      - name: Commit changes
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Enable advanced admin [skip ci]"
          git push
